// const request = require('supertest')
// const app = require('../index')

describe('helloWorldAsync server route', () => {
  it('should respond with the text "Hello!"', () => {
    // request(app)
    //   .get('api/v1/helloWorldAsync')
    //   .expect(200)
  })
})
